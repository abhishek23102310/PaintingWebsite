apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paintingwebsite-chart.fullname" . }}-test-connection"
  labels:
    {{- include "paintingwebsite-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paintingwebsite-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
